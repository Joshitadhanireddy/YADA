#ifndef COMMON_H
#define COMMON_H

// Activity level enum
enum class ActivityLevel {
    Sedentary,
    LightlyActive,
    ModeratelyActive,
    VeryActive,
    ExtremelyActive
};

// Gender enum
enum class Gender {
    Male,
    Female
};

#endif // COMMON_H